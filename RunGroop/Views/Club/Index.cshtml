@model IEnumerable<Club>

@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

<div class="container">
    <div class="ros">
        @foreach(var item in Model)
        {
            <div class="col-md-4">
                <div class="card mb-4 box-shadow">
                    <img class="card-img-top">
                    <div class="card-body">
                        <p class="card-text">@item.Title</p>
                        <p class="card-text">@item.ClubCategory</p>
                        <p class="card-text">@item.Description</p>
                        <div class="btn-group">
                            <a asp-controller="Club" asp-action="Detail" asp-route-id="@item.Id" type="button" class="btn btn-sm btm-outline-secondary">View</a>
                            <button type="button" class="btn btn-sm btm-outline-secondary">Edit</button>
                        </div>
                    </div>
                </div>
            </div>
        }
    </div>
</div>